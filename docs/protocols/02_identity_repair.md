# Protocol amendment triggered by source-identity audit

The first suite is retained as a diagnostic of the public matrix, not the final biological analysis. No outcome of repaired models was available when these rules were fixed.

Original premerge master:12,652records. Raw ADMET output:12,652rows. The actual invalid ADMET record is zero-basedrow9115, ligand_id11434. The public master instead omits ligand_id9116, while its ADMET block exactly follows the raw-valid-output sequence. There are1,678wrong compound/ADMET associations, covering publishedIDs9117–11434. This is independently established by exact numerical equality of all120ADMET columns to the preserved source sequence plus the original master IDs;2,079SMILES differences alone would overcount because charge-state representations can also differ.

Repair joins by stableligand_id after establishing correspondence against the premerge master and original output. For every retained record require either exact canonical isomericSMILES identity (12,193records) or a unique identical RDKit-uncharged isomeric parent in both source and master (391records). Keep these two categories distinct. No stereo removal or tautomer/redox equivalence is used for acceptance. Exclude67unresolved structure transformations and the actual invalidrecord11434. Restore validligand9116; include1298. Primary audited cohort:12,584. Strict exact-identity sensitivity:12,193. No exclusion depends on LD50 or prediction error. The candidate full12651recovery is retained only for inspection and is NOT treated as an audited primary dataset.

All earlier statistical conventions remain. Regenerate new Butina partitions on the audited cohort with the previously chosen cutoffs and seed list; repeat the full five-seed confidence comparisons, matched-size random controls, cutoff sensitivity and localization controls. Repeat the original-test analogue-removal control on the historical ID sets intersected with the audited cohort (restored9116 has no historical split assignment and is absent from this reference-only comparison). These historical references are not primary results.

Recalculate the five global feature-set models on the repaired cohort with the published frozen hyperparameters. Use proper-training data only, with early stopping confined to an internal grouped subset; reserve the calibration and test partitions. Do not claim a new Optuna search was conducted. Use the same five primary structural splits. Recompute point metrics, global prediction intervals, and primary-split feature-attribution summaries. Historical pretrained externalADMET training-set provenance remains unavailable.

The original API request manifest is unavailable; correspondence is not accepted merely on row position. Every included pairing passes the independent structural identity tests above. Source and repaired tables and inclusion manifests are retained; original files are untouched.

Execution detail fixed before the extended runs: strict-identity sensitivity uses the five primary cluster assignments restricted to exact-identity records, preserving assignment of all shared molecules. Global-model early stopping uses an internal 10% group holdout, seed42. Imputation and PCA fit the internal fitting part only; after selecting tree count, refit on the full proper-training partition using these frozen transformations. Calibration/test remain untouched. SHAP uses up to512 test molecules selected with seed20260907, descriptively, after fitting. Frozen published hyperparameters may themselves reflect prior exposure to this collection; these are retrospective robustness checks, not a newly independent external validation.

After observing the repaired ADME–Plain ranking, add a targeted identity robustness check: refit these two fixed model configurations on all five exact-identity partitions. This is explicitly an outcome-triggered follow-up, not a prespecified test. It addresses the particular concern that charge-parent matching might explain the changed ADME result, since charge-related features have high attribution. It is not used to select the primary cohort or split.

A second outcome-triggered, no-refitting diagnostic applies the unchanged joint localizer to the saved global-comparison Plain and ADME predictions on all five primary partitions. This tests whether the local-calibration result is tied to the weaker fixed structural confidence predictor after the global ranking changed. Use the same training-selected biological coordinates and scaler, bandwidth0.9, effective-sample fallback and held-out calibration records. Do not select the predictor or localizer by these new test outcomes; report both configurations and all partitions.
